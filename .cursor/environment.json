{ "install": "python3 -m pip install -U pip && python3 -m pip install -r crypto_backtest/requirements.txt && python3 -m pip install -U pytest", "terminals": [{ "name": "Tests (smoke)", "command": "python3 -m pytest -q" }] }
